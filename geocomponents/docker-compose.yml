services:
  db:
    image: postgis/postgis:16-3.4
    environment:
      POSTGRES_USER: geocomponents
      POSTGRES_PASSWORD: geocomponents
      POSTGRES_DB: geocomponents
    ports:
      - "55432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U geocomponents"]
      interval: 3s
      timeout: 3s
      retries: 20
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
